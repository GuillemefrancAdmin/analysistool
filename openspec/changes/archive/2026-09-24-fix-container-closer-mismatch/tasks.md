## 1. Size the problem

- [x] 1.1 Scan the corpus's existing `.raw.txt` orphans and/or re-run the full repair chain against saved `architecture_spec_writer.txt` intermediates (same approach as `fix-unescaped-quote-json-repair`'s task 3.2) to measure how many real files hit this specific "JsonToken End{Array,Object} is not valid for closing JsonType {Object,Array}" pattern, distinct from other failure classes. Current intermediates all already parse (they reflect the latest, often successfully-retried attempt) -- the real signal is in the 59 `.raw.txt` captured-failure files: running the current repair chain against them, **48/59 (81.4%) fail specifically with this bracket-mismatch pattern** -- the dominant failure mode by far among captured failures, not an edge case. Remaining 11: 6 already parse with the current chain, 5 are one-off distinct errors (boolean parsing, stray `:`, `Infinity` literal, stray `p`, stray `"`).

## 2. Repair function (`scripts/run_analysis_pipeline.ps1`)

- [x] 2.1 Add `Repair-MismatchedContainerClosers`: same stack-tracking walk as `Repair-JsonTruncation` (push `}`/`]` on `{`/`[`, skip string contents respecting backslash escapes), but on each closer encountered, compare against `$stack.Peek()` — if it matches, emit unchanged and pop; if not, emit the expected closer instead and pop. Build via `StringBuilder`. Implemented by always emitting the stack's popped expected closer (equivalent to "emit unchanged if it matches, correct it if not," without needing an explicit comparison, since a matching closer's expected value already equals what was seen).
- [x] 2.2 Insert into the repair chain before `Repair-JsonTruncation`: `(Repair-JsonTruncation (Repair-MismatchedContainerClosers (Repair-UnescapedEmbeddedQuotes (Repair-JsonEscapes (Remove-CodeFence $rawReport)))))`.
- [x] 2.3 Confirm it's a no-op on already-valid JSON (unit tests: simple case, realistic nested-object case, and the real captured failing example from `dependencies_and_integrations`). All 4 tests pass under both PS5.1 and PS7: simple mismatch case now parses, the real captured `dependencies_and_integrations` example now parses correctly (db_interactions count preserved, trailing field intact), and two no-op checks (nested realistic JSON via real `ConvertTo-Json`, and a string value containing brace-like characters) both confirm byte-for-byte (`-ceq`) unchanged output on valid input.

## 3. Prompt reinforcement

- [x] 3.1 Add one sentence to `$ArchitectureSpecSystemPrompt`'s existing instructions: each container's closing bracket must match its own opening bracket type, regardless of what was most recently closed inside it.
- [x] 3.2 Reinforce `database_interactions.operation_type` (and enum fields generally) against the observed `"SELECT"` (not a valid enum value) case, as a concrete example alongside the existing general enum-conformance instruction.

## 4. Verification

- [x] 4.1 Test the repair function against the real captured failing file's raw output (the `dependencies_and_integrations` example in this change's proposal) — confirm it now parses. Covered by task 2.3's unit test 2, which used the exact real captured example verbatim: parses correctly, `database_interactions` count and trailing field both intact.
- [x] 4.2 Run the full repair chain (with this addition) against a broad sample of real `architecture_spec_writer.txt` intermediates to confirm no regressions, same approach as `fix-unescaped-quote-json-repair` task 3.2. Ran against the entire corpus, both directions: all 1578 currently-successful intermediates still parse (zero regressions), and 54/59 (91.5%) of the previously-captured `.raw.txt` failures now parse -- exactly the 48 bracket-mismatch cases plus the 6 that already worked. The remaining 5 are the distinct, unrelated one-off failure types identified in task 1.1 (Infinity literal, stray `p`/`:`/`"`, boolean parsing) -- correctly untouched, out of this change's scope.
- [x] 4.3 Decide, based on task 1.1's prevalence measurement, whether a retroactive backfill+re-run is worth it now or worth bundling with `harden-manifest-against-bitflip-corruption`'s changes into one combined pass. **Decision: no retroactive re-run.** This fix improves *reliability of reaching* a successful parse (fewer retries needed), not the *content* of already-successful files — unlike `fix-architecture-spec-synthesis`/`complete-architecture-spec-schema-coverage`, which changed what gets written. The corpus is already fully completed (1578/1578, confirmed via `queue_eta.ps1`); every currently-completed file already reflects a successful parse (via retry, if needed) regardless of this fix. The benefit is entirely forward-looking (fewer wasted retries/restarts on any future run touching these files again). `harden-manifest-against-bitflip-corruption` (next) doesn't need a pipeline re-run either -- it's validated by replaying saved manifest corruption backups, not by processing source files. No combined re-run needed for either change.
