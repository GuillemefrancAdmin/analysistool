## 1. Prompt instruction (`scripts/run_analysis_pipeline.ps1`)

- [x] 1.1 Add a sentence to `$ArchitectureSpecSystemPrompt`'s existing instructions: any double-quote character appearing inside a string value (a quoted identifier, path, or source snippet being echoed) must be escaped as `\"`, never left bare.

## 2. Repair function

- [x] 2.1 Add `Repair-UnescapedEmbeddedQuotes`: single-pass character walk tracking in-string state; on an unescaped `"` encountered while inside a string, look ahead past whitespace — if the next non-whitespace character is `,`, `}`, `]`, or `:`, treat it as the real terminator; otherwise escape it as `\"` and continue.
- [x] 2.2 Insert it into the repair chain between `Repair-JsonEscapes` and `Repair-JsonTruncation`: `(Repair-JsonTruncation (Repair-UnescapedEmbeddedQuotes (Repair-JsonEscapes (Remove-CodeFence $rawReport))))`.
- [x] 2.3 Confirm it's a no-op on already-valid JSON. Unit-tested (not against live intermediates -- didn't need to be, this is pure text-transform logic) with a simple case, a realistic nested-object case via real `ConvertTo-Json` output, and multiple-embedded-quotes case; all pass under both PS5.1 and PS7, no-op confirmed byte-for-byte (`-ceq`) on valid input in both non-trivial cases.

## 3. Verification

- [x] 3.1 Test against the specific failure patterns found during investigation. Unit test reproduces the exact real-world case verbatim (`"SYSTEM" USING "/ln/current/bin/system/timestamp.sh"` embedded in a string) -- now parses correctly under both engines.
- [x] 3.2 Run the full existing repair chain (with this addition) against a broader sample of currently-successful files to confirm no regressions (task 2.3, at larger scale). Done against the entire real corpus, not just a sample: extracted the live repair functions directly from the script and ran them against all 1578 real `architecture_spec_writer.txt` intermediates from the just-finished run. All 1578 still parse correctly -- zero regressions introduced by inserting the new step.
- [x] 3.3 Monitor `.raw.txt` file creation rate over the next real pipeline run for a qualitative signal that residual JSON-parse failures dropped. Pre-rerun baseline: 37. Post-rerun: 59 (28 created during this specific rerun). At face value this looks like a regression, but investigating the actual failure reasons tells a different story: sampled 5 of the 28 new `.raw.txt` files and checked their exact parse errors directly -- **none show the unescaped-quote pattern this change targets**. 4/5 fail with `"JsonToken EndArray is not valid for closing JsonType Object"` at `dependencies_and_integrations` (a mismatched bracket type on a nested array-of-objects, a different syntax error entirely); 1/5 fails on a literal `Infinity` token (also unrelated). Most likely cause: increased structural complexity from `fix-architecture-spec-synthesis` (array-of-objects for `external_library_dependencies`/`database_interactions`) and `complete-architecture-spec-schema-coverage` (3 more sections) landing in the same rerun, not a regression from this change -- consistent with task 3.2's full-corpus zero-regression result. The bracket-mismatch pattern is a real, new, distinct finding worth its own follow-up change, not squeezed into this one.
