## 1. Prompt additions (`scripts/run_analysis_pipeline.ps1`)

- [x] 1.1 Add `source_evidence` to `$ArchitectureSpecSystemPrompt`'s shape (`symbol_name`, `source_line_start`, `source_line_end`, `analysis_basis` enum, `confidence_level`, `evidence_summary`), using the bracketed-placeholder-token format-template convention.
- [x] 1.2 Add `execution_context` (`entrypoint_type` enum, `trigger_or_invocation`, `runtime_environment`, `config_files_used`, `environment_variables_used`), same convention.
- [x] 1.3 Add `interface_contracts` (`input_schema`, `output_schema`, `data_formats`, `parameter_validation_rules`, `default_and_null_handling`), same convention.
- [x] 1.4 Add one sentence to the existing anti-echo instruction reinforcing enum conformance: every enum-typed value must be exactly one of its listed options.
- [x] 1.5 Cross-check each new section against `templates/source-code-analysis-schema.json` field-by-field (required properties, enum values), same discipline as `fix-architecture-spec-synthesis`'s task 1.5. Verified: all required properties and enum values match exactly.

## 2. token_usage injection

- [x] 2.1 Immediately after the existing `$parsed.module_metadata.file_path = $relativePath` line, add `$parsed | Add-Member -NotePropertyName token_usage -NotePropertyValue $state.token_usage -Force`.
- [x] 2.2 Confirm `$state.token_usage`'s shape at that point in the function satisfies the schema's `token_usage` requirements (`run_total_tokens`, `run_total_elapsed_seconds`, `agents` keyed by name with `model_name`/timing/token fields) without further transformation. Confirmed via schema + `Set-AgentTiming`/`New-EmptyAgentUsage` cross-check -- shapes match exactly, no transformation needed.

## 3. Verification

- [x] 3.1 Out-of-band validation (same approach as `fix-architecture-spec-synthesis` task 4.6): test the updated prompt against 2-3 real files' saved intermediates, confirm `source_evidence`/`execution_context`/`interface_contracts` come back as real, file-specific content, not placeholder echoes. Done: 2 of 3 files produced solid, specific, real content (no echoed placeholder tokens in any of the three). The third came back mostly empty/low-confidence (`confidence_level: 0.0`) for these new fields specifically -- not a placeholder-echo regression (the anti-echo fix worked, nothing was copied verbatim), just weaker extraction on that one file, plausibly ordinary temperature-0.2 model variance rather than a systemic issue. Worth watching in the real run's aggregate numbers (3.6), not blocking.
- [x] 3.2 Run `verify_synthesis_quality.ps1` to record a fresh baseline before shipping. Taken now that `fix-architecture-spec-synthesis`'s own re-run finished (1578/1578): 5.8% whole-document schema-valid (up from 0% pre-that-fix), 64.9% of files still missing the 4 top-level sections this change targets. Also confirms `fix-architecture-spec-synthesis` itself landed cleanly: `functional_requirements` 95.0% real (was ~0.1%), `technical_debt_and_code_smells` 89.4% real (was ~0.4%), zero placeholder/wrong-shape remaining on either field.
- [x] 3.3 Ship the changes. Code implemented and syntax-verified under both PS5.1 and PS7.
- [x] 3.4 Run `backfill_new_stage.ps1 -NewStageName architecture_spec_writer -EvenIfAlreadyRun` report-only, confirm target count, then `-Force`. Report-only confirmed 1578/1578; `-Force` run completed: 1578/1578 rewound, manifest confirmed healthy (0 completed, 1578 queued, 0 blocked, 0 in_progress).
- [x] 3.5 Re-run the pipeline. User ran `run_analysis_pipeline_parallel.ps1`; completed 1578/1578 (survived, and needed manual recovery from, an unrelated hard worker-process crash and a manifest bit-flip corruption incident along the way -- both handled without data loss; see 3.6 for the resulting numbers).
- [x] 3.6 Run `verify_synthesis_quality.ps1` again; confirm the whole-document `Test-Json` pass rate has moved meaningfully off 0%, and spot-check a few files' `source_evidence`/`execution_context`/`interface_contracts`/`token_usage` by hand. Whole-document schema-valid: 5.8% -> 15.7%; **0% of files now missing any of the 4 target top-level sections** (was 64.9%) -- the primary goal is fully met. Remaining schema-invalid files fail for other, pre-existing reasons (e.g. a newly-observed bracket-type-mismatch issue on `dependencies_and_integrations`, unrelated to this change -- noted as a follow-up). Hand spot-check plus a full-corpus fill-rate measurement: `token_usage.run_total_tokens` populated in 100% of files (script-injected, fully reliable as designed); `source_evidence.symbol_name` 55.7%, `execution_context.entrypoint_type` 67.9%, `interface_contracts.input_schema` only 6.2% non-empty -- all three are present in every file (no missing-section failures) but the LLM often leaves them empty rather than placeholder-echoing into them, ordinary extraction difficulty rather than a regression of the anti-echo fix.
